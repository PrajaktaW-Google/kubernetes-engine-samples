apiVersion: inference.networking.x-k8s.io/v1alpha2
kind: InferenceModel
metadata:
  name: vllm-llama3-8b-instruct
  labels:
    inference.networking.x-k8s.io/model-type: llm
    inference.networking.x-k8s.io/framework: vllm
spec:
  modelSource:
    repository: huggingface.co
    modelId: meta-llama/Meta-Llama-3-8B-Instruct
    revision: main
  resources:
    requests:
      nvidia.com/gpu: 1
    limits:
      nvidia.com/gpu: 1